<% provide(:title, 'All projects') %>
<h1>All Projects</h1>
<% if signed_in? && current_user.admin? %>
<%= button_to('Add Project', new_project_path, :class => "btn" )%>
<% end %>

<%= will_paginate %>

<table class="table table-striped">
  <tr>
  	<th>Project</th>
  	<th>#Servers</th>
  	<th>Comment</th>
  	<% if signed_in? && current_user.admin? %><th>Actions</th><% end %>
  </tr>
  <%= render @projects %>
</table>

<%= will_paginate %>

<br>
<% if signed_in? && current_user.admin? %>
<%= button_to('Add Project', new_project_path, :class => "btn" )%>
<% end %>
